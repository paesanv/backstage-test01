{
  "name": "{{ parameters.name }}",
  "version": "1.0.0",
  "description": "{{ parameters.description }}",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "dependencies": {
    "express": "^4.17.1"
  }
}
